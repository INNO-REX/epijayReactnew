version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - epijay-network
    restart: unless-stopped

networks:
  epijay-network:
    driver: bridge